import {defineConfig} from "vite";

export default defineConfig({
    base: "/db-my-routes/"
    // Uncomment to use JSX:
    // esbuild: {
    //   jsx: "transform",
    //   jsxFactory: "m",
    //   jsxFragment: "'['",
    // },
});
